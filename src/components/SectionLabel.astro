---
interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<p class={`text-xs font-semibold tracking-[0.15em] uppercase text-muted-foreground ${className}`.trim()}>
  <slot />
</p>
